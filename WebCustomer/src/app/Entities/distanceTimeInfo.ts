/**
 * Clase que se encarga de contener la información
 * distancia y tiempo que de semora un recorrido
 *
 * @export
 * @class DistanceTimeInfo
 */
export class DistanceTimeInfo {
  public id: number;
  public distance: number;
  public duration: number;
}
